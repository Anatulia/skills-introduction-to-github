# Trascrizione — scegli almeno un motore:
faster-whisper>=1.0.0   # default; modelli da HuggingFace
sherpa-onnx>=1.10.0     # alternativa; modelli da GitHub (proxy che bloccano HF)
numpy>=1.24.0           # richiesto dal motore sherpa

# Lettura visiva — OPZIONALI
Pillow>=10.0.0          # gestione immagini per l'OCR
pytesseract>=0.3.10     # OCR (richiede il binario `tesseract` di sistema)
anthropic>=0.40.0       # descrizione vision via Claude (richiede ANTHROPIC_API_KEY)

# Dipendenze di SISTEMA (non installabili via pip):
#   ffmpeg, ffprobe   -> estrazione frame e scene-detection
#   tesseract-ocr     -> OCR (con pacchetto lingua: tesseract-ocr-ita)
# Debian/Ubuntu: apt-get install ffmpeg tesseract-ocr tesseract-ocr-ita
